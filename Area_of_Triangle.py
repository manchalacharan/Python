'''The area of triangle formula is
        area = (b*h)/2
    where b is length
          h is height
    to display the area of triangle value'''

length = int(input("Enter the b value: "))
height = int(input("Enter the h value: "))

area = (length*height)/2

print("Area of a Triangle is: ",area)
